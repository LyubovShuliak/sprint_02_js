const beginRange = +prompt("", "")
const endRange = +prompt("", "")

function checkDivision(beginRange, endRange) {
  if (beginRange == NaN || endRange == NaN) {
    beginRange = 1
    endRange = 100
  }
  return function() {
    for (let i = beginRange - 1; i < endRange; i++) {
      let
      return

    }

  }

}